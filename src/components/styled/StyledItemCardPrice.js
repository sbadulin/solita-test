import styled from "styled-components";

import { COLORS } from "../../constants";

export const StyledItemCardPrice = styled.div`
  font-family: Playfair Display, serif;
  font-style: normal;
  font-weight: normal;
  line-height: 60px;
  font-size: 36px;
  text-align: right;
  letter-spacing: -0.54px;
  color: ${COLORS.MAIN};
`;
